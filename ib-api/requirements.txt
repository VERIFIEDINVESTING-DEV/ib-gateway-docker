# FastAPI and ASGI server
fastapi>=0.109.0
uvicorn[standard]>=0.27.0

# Form data parsing (required for OAuth2PasswordRequestForm)
python-multipart>=0.0.6

# Interactive Brokers TWS API
ibapi
# JWT Authentication
python-jose[cryptography]>=3.3.0

# Password hashing (using bcrypt directly, not passlib due to compatibility issues)
bcrypt>=4.0.0

# Settings management
pydantic-settings>=2.0.0
